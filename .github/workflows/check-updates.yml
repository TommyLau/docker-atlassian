name: Check Atlassian Updates

on:
  schedule:
    # Run daily at 8:00 AM UTC (4:00 PM Shanghai)
    - cron: '0 8 * * *'
  workflow_dispatch:  # Allow manual trigger

jobs:
  check-and-update:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      actions: write  # Needed to trigger other workflows
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Check for updates
        id: check
        run: |
          python3 check-versions.py --update 2>&1 | tee update-log.txt
          
          # Check if Dockerfiles changed
          if git diff --quiet -- '*/Dockerfile'; then
            echo "has_dockerfile_updates=false" >> $GITHUB_OUTPUT
          else
            echo "has_dockerfile_updates=true" >> $GITHUB_OUTPUT
            UPDATED=$(git diff --name-only -- '*/Dockerfile' | sed 's|/Dockerfile||' | tr '\n' ' ' | xargs)
            echo "updated_products=${UPDATED}" >> $GITHUB_OUTPUT
          fi
          
          # Check if cache changed
          if git diff --quiet -- 'lts-cache.json'; then
            echo "cache_updated=false" >> $GITHUB_OUTPUT
          else
            echo "cache_updated=true" >> $GITHUB_OUTPUT
          fi

      - name: Commit Dockerfile updates
        if: steps.check.outputs.has_dockerfile_updates == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          
          UPDATED="${{ steps.check.outputs.updated_products }}"
          
          git add '*/Dockerfile' lts-cache.json
          git commit -m "🔄 Auto-update: ${UPDATED}" -m "$(cat update-log.txt | grep -E '(📝|🔄|✅)' | head -20)"
          git push

      - name: Commit cache updates only
        if: steps.check.outputs.has_dockerfile_updates == 'false' && steps.check.outputs.cache_updated == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          
          git add lts-cache.json
          git commit -m "📋 Update LTS cache"
          git push

      - name: Trigger Docker build workflow
        if: steps.check.outputs.has_dockerfile_updates == 'true'
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: 'docker-build-push.yml',
              ref: 'master'
            });
            console.log('Triggered docker-build-push workflow');

      - name: Summary
        run: |
          echo "## 📊 Check Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          if [ "${{ steps.check.outputs.has_dockerfile_updates }}" == "true" ]; then
            echo "### ✅ Updates Applied" >> $GITHUB_STEP_SUMMARY
            echo "Updated: ${{ steps.check.outputs.updated_products }}" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "🚀 Triggered docker-build-push workflow" >> $GITHUB_STEP_SUMMARY
          else
            echo "### ✅ All products up to date" >> $GITHUB_STEP_SUMMARY
          fi
          
          if [ "${{ steps.check.outputs.cache_updated }}" == "true" ]; then
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "📋 LTS cache updated" >> $GITHUB_STEP_SUMMARY
          fi
